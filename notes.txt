brew update && brew upgrade ruby-build
rbenv install 2.5.0
gem install bundle
rails db:setup
git config --local user.name 'alex'
git config --local user.email 'a.plyushchay@gmail.com'








git commit -am "make it better"
git push heroku master
